# Day 6: Functions, Return, and Scope

**Date:** October 11, 2025  
**Focus:** Learning how to define, call, and return values from functions with a deeper look at scope and nested functions.

---

## Topics Covered  

- Function declaration and invocation  
- Return values and how they work  
- Functions with and without parameters  
- Nested functions and variable scope  

---

## Exercises Completed  

### Exercise 1: Square It!  
Created a `square(num)` function that returns the square of a number.  
Called it three times with different numbers and logged the results.  
File: `square.js`  

### Exercise 2: Make Some Noise  
Defined a `makeNoise()` function that prints “Beep! Beep! JavaScript is working!” twice.  
Also created a `sayHello(name)` function that prints personalized greetings.  
File: `makeNoise.js`  

### Exercise 3: Rounding Logic  
Implemented the `roundTo(n, step)` function to round numbers to the nearest step, using modular arithmetic.  
Tested it with `roundTo(27, 10)` and `roundTo(23, 5)` and printed results.  
File: `roundTo.js`  

### Exercise 4: Nested Functions (Hummus Builder)  
Recreated the hummus example to understand nested functions and scope.  
Modified it by adding some new ingredients
File: `hummus.js`  

---

## Code Summary  

- Strengthened understanding of how functions return and reuse logic.  
- Learned to handle both single and multiple parameters.  
- Practiced modular arithmetic and string concatenation.  
- Gained clearer insight into scope and nested function design.  

---

## Faith Reflection  

Programs run effectively because some other function inside it is running perfectly so also in life, no man is an island we need Jesus inside our spirit to be perfectly ok for our lives to run well.

>*neither is there salvation in any other for there is none other name under heaven given among men whereby we must be saved*  Acts 4:12 (KJV)